# .env.sample
# This is a template. Copy this to a file named .env and fill in your actual secrets.
# IMPORTANT: DO NOT COMMIT the .env file to Git.

# --- Agent & LLM Configuration ---
# Get this from Google AI Studio (https://aistudio.google.com/app/apikey)
GOOGLE_API_KEY="your_google_ai_studio_api_key_here"

# --- Google Cloud Platform Credentials (for Drive, Sheets, Calendar) ---
# We will create this in the next step. It will be a large JSON string.
GOOGLE_CREDENTIALS_JSON='{"type": "service_account", ...}'

# --- n8n Configuration ---
# We will set these up when we configure n8n
WEBHOOK_URL="your_n8n_webhook_url_here"

# --- WhatsApp API Credentials ---
# We will get these when setting up the WhatsApp Business API
WHATSAPP_ACCESS_TOKEN="your_whatsapp_token_here"
WHATSAPP_PHONE_NUMBER_ID="your_phone_number_id_here"